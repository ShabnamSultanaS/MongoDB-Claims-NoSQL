# MongoDB-Claims-NoSQL
Every relational schema I've built (SQL Server, DuckDB) starts from normalisation and joins. Document databases invert that: you design around how the data is *read*, and you deliberately duplicate data to avoid joins. Understanding that trade-off in practice, not just in theory, was the point of this project.
